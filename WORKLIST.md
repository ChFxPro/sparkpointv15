# SparkPoint master worklist

**Single source of truth for planned, active, and completed website work.**

## How to use this document

- Update this file at the beginning and end of every substantive work session.
- Keep only actionable work here; link to reports, PRs, and supporting documents instead of duplicating their full contents.
- Work in priority order unless an owner explicitly changes it. Bundle related items into one focused, testable change set.
- Status: `Now`, `Next`, `Blocked`, `Monitoring`, `Done`.
- Priority: `P0` urgent production or data risk; `P1` important security/reliability; `P2` planned improvement; `P3` editorial/polish.
- Each completed item should record the date, verification, and the PR or deployment that delivered it.

## Active bundles

### P0 — Production trust and deployment safeguards

| ID | Status | Work | Owner / next action | Completion evidence |
| --- | --- | --- | --- | --- |
| SEC-001 | Done | Custom domain / TLS on GitHub Pages. | Pages "DNS Check Successful" 2026-07-22 (4 AAAA records added; apex serves HTTPS, www+http redirect to https apex). CNAME-persistence PR #77 deploying. Squarespace shows apex "not connected / no certificate" = EXPECTED (hosted on GitHub Pages, not Squarespace). 2026-07-26: Pages flagged "DNS Check In Progress" again (`https_certificate.state: dns_changed`, per `gh api repos/ChFxPro/sparkpointv15/pages`) — apex kept serving HTTPS throughout (existing cert valid to 2026-09-13), no repo/deploy change involved; correlates with Squarespace-side subdomain activity for TICKET-001 (Squarespace republishes the whole zone on subdomain connect/disconnect). Expected to self-clear; no DNS action taken. | Pages DNS check green; confirm Enforce HTTPS. |
| SEC-002 | Done | Enable effective protection for `main`. | `Protect main (production)` ruleset enabled 2026-07-21 (PRs + passing build required, force-push/deletes blocked). | Ruleset active enforcement confirmed by owner. |
| SEC-003 | Done | Enable GitHub secret scanning and push protection. | Secret scanning + push protection enabled 2026-07-21. | GitHub security settings confirmed enabled by owner. |

### P1 — Public form and platform hardening

| ID | Status | Work | Owner / next action | Completion evidence |
| --- | --- | --- | --- | --- |
| SEC-004 | Next | Harden public intake submissions. | Server-side validation and a 16 KB request limit deployed 2026-07-17; select and configure rate limiting and a bot challenge. | Production rejection check passed; remaining abuse controls verified. |
| SEC-005 | Next | Add browser security headers at the edge. | Choose/configure an edge layer capable of CSP, frame protection, `nosniff`, referrer, and permissions policies; deploy CSP report-only before enforcement. | Live response-header check and no blocked core site features. |
| SEC-006 | Next | Tighten GitHub Actions supply-chain controls. | Deployment workflow actions pinned to reviewed full SHAs on 2026-07-17; restrict allowed actions and configure Dependabot updates. | Actions settings and successful Pages deployment. |
| SEC-008 | In progress | Refresh privacy and retention disclosure. | Public /privacy page redesigned + accurate copy SHIPPED (merged PR #74). Internal data-processing/retention record drafted (project doc SparkPoint-Privacy-and-Data-SEC008.md). Site confirmed tracker-free. REMAINING: owner/counsel confirm retention window (default 24mo) + named data owner (default info@yoursparkpoint.org). | Owner/counsel sign-off. |
| IP-001 | Next | Establish SparkPoint intellectual-property and brand protections. | Asset inventory completed 2026-07-17 in `docs/IP-ASSET-RIGHTS-INVENTORY.md`; next, confirm ownership/permissions for code, copy, photography, logos, and contractor work; decide repository licensing posture; complete trademark clearance/filing strategy and publish reviewed rights notices. | Asset-rights inventory complete; remaining: board/counsel approval and published notices. |

### P2 — Preventive code quality

| ID | Status | Work | Owner / next action | Completion evidence |
| --- | --- | --- | --- | --- |
| SEC-007 | Next | Remove or constrain raw HTML story rendering. | Keep content code-authored, or centralize allowlist sanitization before any CMS/user content is introduced. | Unit coverage or documented trusted-content boundary. |
| OPS-001 | Next | Add automated security checks. | Add dependency audit and a non-secret-leaking scan to CI after Actions policy is hardened. | Required checks run on a PR. |

## Ongoing product work

| ID | Status | Work | Bundle / notes |
| --- | --- | --- | --- |
| SEO-001 | Done | Fix GSC "Page indexing" report showing 33 "Not found (404)" + 4 "Page with redirect" + 1 noindex + 31 "Discovered" + 4 "Crawled — not indexed" (73 not-indexed total, per owner's 2026-08-26-window GSC export). | Root-caused by cross-referencing the export against live Search Console (owner's logged-in session) and the codebase: (1) `scripts/prerender.mjs`'s `SKIP` set left `/news-media`, `/newsroom`, `/commconn`, `/resiliency-hub`, `/volunteer`, `/partner`, `/contact`, `/healthcare-story` unprerendered — GitHub Pages served a real HTTP 404 before the client-side `Navigate` ever ran (same bug already fixed for `/sponsors` in PR #96, now generalized to the rest). Moved all of them into the `REDIRECTS` map so each gets a real static 200 stub (noindex + canonical + meta-refresh). (2) `/donations` and `/newsletter` in that same map had drifted to the old raw `cowbell-primrose-tet2.squarespace.com` host — App.tsx was repointed to `secure.yoursparkpoint.org` back in commit 2f0b17f but the prerender map wasn't; fixed to match. (3) Added redirect stubs + `Navigate` routes for legacy Squarespace-era URLs with clear modern equivalents: `/about-us`→`/about`, `/sparkpurpose`→`/programs/purpose-workshops`, `/leadership-wellbeing`→`/programs/leadership-well-being`, `/voice-of-the-students`→`/programs/voice-of-the-students-youth-leadership`, `/workforce-wellbeing`→`/programs/workplace-well-being-positive-culture`, `/wellness-rooted-in-connection` + `/wellnessrootedinconnection`→`/programs/wellness-rooted-in-connection-collaborative-trcn`, `/helene-one-year-of-healing`→`/stories/community-champions/helene-anniversary`, `/media`→`/press`. (4) Cleaned the stale checked-in `public/sitemap.xml` (removed `/news-media` and the noindex `/community-champions/helene-one-year` — the real deployed sitemap is regenerated dynamically by `prerender.mjs` and never had these, but the source file was misleading). **Confirmed NOT bugs** (live-verified via GSC's live URL Inspection test + reading the actual per-issue URL tables, owner approved via chat): the remaining ~20 of the 33 "404" URLs are dead Squarespace-platform URLs with no reasonable modern equivalent (`/new-page-2`, `/pfa`, `/tele-therapy`, `/blog-impact/example-blog-post-*`, `/store/p/rmbudo132p...`, etc.) — expected fallout of the platform migration, not fixable via redirects; all 4 "Page with redirect" URLs are the correct http→https/www→apex redirects plus one stale `/community-connectors` crawl; all 4 "Crawled — not indexed" URLs (`/cart`, `/about-us-1`, `/programs-1`, a UTM-tagged homepage variant) are pre-migration Squarespace artifacts Google is correctly declining to index; most of the 31 "Discovered — not indexed" were `www.yoursparkpoint.org` duplicates of core pages, not new content. Also removed a stale `https://www.yoursparkpoint.org/sitemap.xml` sitemap submission in Search Console (submitted Mar 3, still being re-read as of Jul 26) that was the likely source of that www-duplicate discovery noise — only the correct `https://yoursparkpoint.org/sitemap.xml` (submitted Jul 21) remains registered. Verified via `--dry` prerender runs (all 8 new stub files render correctly with `noindex`/canonical/meta-refresh) and a full production build matching the CI env. REMAINING: after merge/deploy, click "Validate fix" on the "Not found (404)" and "Page with redirect" rows in GSC to trigger recrawl of the whole affected set (stale + now-fixed entries alike). | GSC issue counts drop after the next validation cycle; spot-check via URL Inspection "Test live URL." |
| SEO-002 | Done | Address two further GSC findings: (a) deep content pages (stories, programs, resource-directory entries) get almost no internal links; (b) Resource Directory entries' page titles don't match real search intent. | (a) Added related-content cross-links to `StoryArticlePage.tsx` (related stories), `ProgramDetailPage.tsx` (new `RelatedPrograms` section/component, wired via `sections/index.ts`), and `DirectoryEntryPage.tsx` (related directory entries) — each surfaces a handful of contextually relevant same-type pages so deep pages Google previously reached almost exclusively via the sitemap now have real internal inlinks. (b) Added a `seoTitle` field to `src/data/resources.ts` for Resource Directory entries and wired `DirectoryEntryPage.tsx` to use it for the page `<title>`/meta when present, so partner-organization searches (GSC showed exact-name queries getting impressions but zero clicks) land on a title that actually matches the org name instead of a generic directory-entry title. Produced via an orchestrated multi-agent workflow: two separate isolated agents worked the cross-linking and directory-title changes independently, then were merged and build-verified together before shipping (full production build, 100 pages prerendered, 0 failed). | Full production build with 0 failed prerendered pages; spot-check related-links sections render and `seoTitle` appears in rendered `<title>` for directory entries that set it. |
| SEO-003 | Now | Follow-up to SEO-001 on the 2026-08-13 GSC Pages export (52 not-indexed): consolidate remaining legacy Squarespace URLs, de-duplicate the Helene one-year story, and stop local builds from silently disagreeing with production. | **(1) Reversed SEO-001's `noindex` on redirect stubs.** GitHub Pages cannot issue a real 301, so the canonical + 0s meta-refresh is the *only* signal consolidating a retired URL's ranking value onto its replacement; pairing it with `noindex` is contradictory and Google drops the URL instead of passing equity forward. The stubs stay out of `sitemap.xml` regardless, so they are still not crawled as standalone content. This is a deliberate reversal of a documented SEO-001 decision, not an oversight fix. **(2) Six new redirect stubs + `Navigate` routes:** `/community-connections`→`/community-connectors` (note: at the root, NOT under `/programs/` as the GSC remediation plan assumed), `/home-impact`→`/impact`, `/trauma-resilient-communities-partnership`→`/programs/wellness-rooted-in-connection-collaborative-trcn`, `/cognitivehealthresources`→`/resources/know-your-numbers`, `/supported`→`/programs/supported-connected-classrooms`, and `/stories/disaster-recovery/helene-one-year`→`/stories/community-champions/helene-anniversary`. `/supported` is the retired URL for **SupportEd** (Connected Classrooms & Educator Well-Being), not a supporters page — confirmed against the 2026 program inventory, which independently flagged it as a live 404 with a broken backlink. **(3) Fixed a pre-existing hard 404:** `/stories/community-champions/helene-anniversary` is a hand-written route in `App.tsx` rather than a `stories.ts` entry, so prerender route-derivation never picked it up and it had no build output at all — meaning SEO-001's `/helene-one-year-of-healing` redirect had been pointing at a 404 since it shipped. Added to `STATIC`. Both Helene URLs were previously live and in the sitemap; `/stories/community-champions/helene-anniversary` is now canonical and the duplicate is gone. **(4) Build-default hardening.** `vite.config.mts` defaulted `base` to `/sparkpointv15/` (the pre-custom-domain project URL) and `defaultSiteOrigin` to `www.yoursparkpoint.org`. CI overrides both, so they only ever reached local builds — where `npm run build` silently produced ~88 byte-identical empty SPA shells (assets referenced at `/sparkpointv15/...` that the prerenderer's root-serving preview server could not load, so React never booted) while still exiting 0. Both defaults now match production, and `prerender.mjs` hard-fails when a majority of routes render empty. A bare `npm run build` now produces a byte-identical sitemap to the CI build. Note SEO-001 observed that most "Discovered — not indexed" entries were `www.` duplicates, which the origin default was quietly reinforcing locally. | Verified: bare `npm run build` and a CI-env build both prerender 109 pages, 0 failed; guard confirmed to fire (60/60, exit 1) on a deliberately misconfigured build and stay silent on a good one; sitemap 81 URLs, byte-identical before/after the default changes; zero `noindex` and zero `www.yoursparkpoint` references in output. REMAINING after merge/deploy: click "Validate fix" on the GSC "Not found (404)" and "Page with redirect" rows to trigger recrawl. |
| SEO-004 | Next | Identify redirect targets for three legacy Squarespace URLs still 404ing, which need owner knowledge of the retired content. | `/connection-corner` (possibly `/stories/community-voice` — naming only, unconfirmed), `/brevard-blue-zones-certification` (possibly `/rural-health-convening` — unconfirmed), and `/donate-vos-funds` ("VOS" unresolved; may relate to Voice of the Students, but the URL suggests a donation fund with no current equivalent). No match exists anywhere in the codebase and no reference was found in the owner's knowledge base, so these were deliberately left out of SEO-003 rather than guessed at — a wrong redirect is worse than a 404 because it sends real visitors and link equity to the wrong page. Distinct from the URLs SEO-001 already adjudicated as intentionally dead (`/new-page-2`, `/pfa`, `/tele-therapy`, `/blog-impact/example-blog-post-*`, `/store/p/...`), which need no action. Next action: owner confirms what each URL used to serve, or confirms they should stay 404. | Either a redirect stub per URL following the SEO-003 pattern, or an explicit owner decision recorded here to leave them dead. |
| EVENT-002 | Done | Add Thrive @ Five: Common Ground Release Party to `/events` and build its dedicated fundraiser page. | Built locally on `codex/thrive-at-five-coffee-release` 2026-07-27; pending PR/deployment. `/events` now features the July 31 fundraiser and retains the Rural Health Convening as the next upcoming event. `/events/thrive-at-five` includes the supplied product photography and label art, exact 50%-of-August-profits language, an August 1 coffee-purchase CTA linking to Pisgah Coffee Roasters, live music/food/friends details, click-to-play privacy-enhanced YouTube embed, Hub vision, partner branding, map/calendar/Facebook links, Event JSON-LD, sitemap and prerender registration. Desktop/mobile browser checks found no overflow or console errors; the purchase section and video interaction were verified. The exact GitHub Pages production build environment (`PUBLIC_BASE=/`) passed, followed by focused prerender validation: 81 pages prerendered, 0 failed; only the repository’s known chunk-size and Node module-type warnings remain. |
| RH-001 | Done | Build the `/rural-health-convening` event portal and Rural Health Field Simulator introduction. | Merged in PR #85 and verified live 2026-07-27 after GitHub Pages deployment run 30297118711. Includes event facts, a temporary Monday.com priority-registration list for the limited 200-seat event, partner hierarchy, simulator journey, ongoing-program framing, event JSON-LD, and static prerender registration. The prior public ticket CTA and QR are intentionally withheld until registration opens. Follow-up 2026-07-28 (PR #93, #94): added Transylvania Regional Hospital as a third sponsor; sponsor logos link to each org's site; corrected sponsor tiering to Summit ($3,500, equal billing — UNC Health Pardee and Transylvania Regional Hospital) vs. Ridgeline ($2,500, visibly smaller — Pisgah Health Foundation), reflected in logo size/order, a new hero credit line, and the SEO description (verified under the 160-char `SEOHead` cap after a reviewer flagged truncation). Verified locally via dev server on desktop and mobile viewports; no console errors. |
| RH-002 | Done | Add Transylvania County Tourism Development Authority as a Highlands ($1,500) sponsor across the convening webpage and official invitation. | Completed 2026-07-30 and merged via PR #95. The supplied transparent Brevard TDA mark now links to Explore Brevard and appears after Summit and Ridgeline sponsors in the hero credit, partnership hierarchy, and Event JSON-LD. Desktop/mobile browser checks confirmed the 120 px → 88 px → 72 px tier hierarchy, proportional 86 × 64 px mobile TDA rendering, no horizontal overflow, and a working simulator anchor. The direct-purchase invitation and email copy now include Highlands support; the purchase URL returned HTTP 200, Vite production compilation passed, and the refreshed one-page PDF was rendered and visually verified at exactly 5 × 7 inches. |
| RH-006 | Done | Remove the temporary secondary “Program Preview” card from Laurie Stradley’s featured regional-update section until confirmed agenda specifics are available. | Completed locally 2026-07-30 on `codex/remove-rh-program-preview`. The entire agenda card, “Time to be announced” placeholder, and 123 lines of unused card/responsive CSS were removed. Laurie’s approved promotional blurb remains as a clean single-column featured update; the invitation was not changed. Vite production compilation and diff checks passed. Browser QA at desktop and mobile widths confirmed the card and placeholder are absent, the headline/blurb remain, body width matches viewport width, the existing simulator experience remains elsewhere on the page, and no relevant console errors appear. |
| RH-003 | Done | Add Laurie Stradley’s approved October 1 Region 1 / NC ROOTS update to the convening page, program presentation, and official invitation. | Completed 2026-07-30, merged in PR #97, and verified after the successful GitHub Pages deployment. The page and invitation carry the approved blurb verbatim and identify Laurie Stradley as CEO of Impact Health while preserving the SparkPoint convening as the primary identity; no Impact Health logo was added. A temporary secondary agenda preview was included initially, then removed locally under RH-006 until confirmed specifics are available. The editable invitation source passed overflow testing; its rendered PNG and one-page 5 × 7 PDF retain the direct-registration QR/short URL, event details, sponsors, partners, and accessibility contact. Pending confirmation remains intentionally deferred: exact agenda time, duration, slide needs, and technical-support arrangement. |
| RH-004 | Done | Replace the first Rural Health Field Simulator gallery image, `public/assets/Rural Health/Screenshot 2026-07-27 at 2.20.40 PM.webp`, with the supplied matching update at `public/assets/Rural Health/simulator_screencap.webp`. | Completed 2026-07-30, merged in PR #97, and verified after deployment. The supplied 1896 × 1642 WebP is published and the gallery reference uses it while preserving the existing alt text, dimensions, and responsive crop behavior. Desktop/mobile rendering, production compilation, GitHub Pages deployment, and public asset delivery passed. |
| RH-005 | Done | Add high-level convening experiences and a subtle Transylvania County visitor invitation to the event page and official invitation. | Completed 2026-07-30, merged in PR #97, and verified after deployment. At the time, confirmed program cues were limited to the participatory Rural Health Field Simulator, cross-sector connection, and shared learning; no unconfirmed sessions or agenda timing were invented. The Highlands sponsor area invites attendees to spend more time locally and links to Explore Brevard. Vite compilation, desktop/mobile browser checks, Explore Brevard destination inspection, PPTX overflow testing, PDF geometry, and final print rendering passed. Superseded 2026-07-31 by RH-007's expanded four-item program lineup. |
| RH-007 | Done | Expand the October 1 program lineup on the convening webpage and official invitation from the three generic experience cues (RH-005) to four named segments, and replace the standalone Laurie Stradley regional-update spotlight with a general "historic moment for rural health in North Carolina" framing panel. | Directed by Jeff Bannister in-session 2026-07-31, who supplied the confirmed lineup: a statewide briefing from Maggie Sauer (NCDHHS, Office of Rural Health), Laurie Stradley's previously-approved NC ROOTS regional update (RH-003), a new story-collection segment, and the Rural Health Field Simulator. Recording this here per a Codex PR review flag: the program lineup itself (who is presenting what) is confirmed by Jeff; the short display labels used in the UI ("Rural Health Briefing," "Story Collection: Listening to Build Connection") are Claude-drafted names pending Jeff's final wording sign-off, not independently confirmed titles. `npm run build` (full production build + 64-page prerender) passed with 0 failures; dev-server DOM/computed-style checks confirmed the dark panel, 2-column grid, and badge styling render correctly, and the mobile viewport collapses to one column with no horizontal overflow. Opened in PR #99; review, merge, and deployment remain. |
| RH-008 | Done | Rework the convening hero section's sponsor/partner display: replace the old duplicate text-credit paragraph + logo row with one consolidated, linked logo element under the map figure; add the NCRHA and FHLI partner marks alongside the four funding sponsors, visually delineated ("Presented by" vs. "In partnership with"); enlarge and reposition the block to better align with the left column; link all NCRHA/FHLI logo instances sitewide. | Iterated in-session 2026-07-31 per Jeff Bannister's direction across several rounds: consolidated the hero's sponsor text list and logo row into a single logos-only linked element in the right column beneath the map figure (replacing a left-column placement used briefly mid-session); added NCRHA + FHLI to the hero as a second, delineated "In partnership with" group (vertical divider on desktop, horizontal on mobile); enlarged all hero sponsor/partner logos (~20%) and increased the gap under the map figure so the block sits closer to the left column's button row; equalized the NCRHA/FHLI logo heights (26px each); linked NCRHA (`ruralhealthnc.org`) and FHLI (`foundationhli.org`) both in the new hero element and in their pre-existing unlinked instances in the "A convening built through partnership" section (SparkPoint's own logo there remains unlinked). Verified via dev-server DOM/computed-style and accessibility-tree checks on desktop (1280px) and mobile (700px) — correct link targets, no wrapping/overflow, no console errors. Screenshot capture was unreliable for one distant scroll position this session (environment glitch, confirmed unrelated to the code via DOM inspection). |
| RH-009 | Done | Produce half-sheet print formats for the current official Rural Health Convening invitation. | Completed 2026-08-03 without changing the invitation content or webpage. Created a native one-page 5.5 × 8.5 PDF and a landscape letter-size 11 × 8.5 PDF containing two identical half-page invitations side by side. Final imposition removes the earlier contain-fit white bands and dashed gutter: each invitation fills exactly 396 × 612 points, the two panels meet at the 5.5-inch centerline, and no typography is stretched. The current four-part program invitation is the authoritative artwork. Both PDFs are one page and passed Poppler geometry, link-annotation, and rendered visual checks. No commit, PR, or deployment requested. |
| RH-010 | Done | Refine the official invitation's sponsor presentation, Laurie Stradley attribution, overview language, and event-information link without changing the convening webpage. | Completed 2026-08-03. Removed sponsor tier-name labels while retaining every sponsor logo and its existing relative hierarchy; changed Laurie's line to `NC ROOTS · Laurie Stradley - Impact Health`; revised the overview to “statewide insights, regional updates, community stories”; and added a clearly labeled, secondary `EVENT DETAILS & FULL PROGRAM` link to the convening page while preserving the QR/short URL as the primary registration action. Regenerated the editable PPTX, 5 × 7 PDF, 5.5 × 8.5 PDF, and two-up letter landscape PDF. Verified the original Fraunces Variable, Fraunces Variable Italic, and Manrope type system; PPTX overflow; exact PDF geometry; clickable links; and rendered layouts. No webpage code, commit, PR, or deployment was requested. |
| RH-011 | Done | Restore the invitation's pre-edit visual fidelity, replace the prior link treatment with one primary QR, and incorporate the final program-panel copy. | Completed 2026-08-03 after the RH-010 PNG/PDF export was found to have substituted and rescaled the intended typography. Restored the full-height Fraunces/Manrope field-atlas composition from the intact high-resolution source; rebuilt the dark program panel as a single clean layer so no old copy remains underneath; balanced its vertical spacing; and removed sponsor tier labels while preserving the four-logo prominence order. The single dominant QR now reads `EVENT DETAILS & TICKETS`, points to `https://yoursparkpoint.org/rural-health-convening`, and replaces the abandoned second-QR/direct-purchase treatment. Added Jeff's final overview and program wording: State of Rural Health; Maggie Sauer, Director, NCDHHS Office of Rural Health; Special Regional Update with Laurie Stradley, CEO, Impact Health; From Story to Insight / Turning Stories into Data; and Rural Healthcare Access Simulation / Experience the journey. Gain insight. Final proofing also removed the exposed old-QR ghost strip, restored the title-rule and vertical-separator continuity, and lowered the QR content within its cleanup panel. Regenerated the production PPTX, PNG, exact 5 × 7 PDF, 5.5 × 8.5 PDF, and two-up landscape-letter PDF. Verified full-page renders, no old-section overlap, no title/logo clipping, no PowerPoint overflow, correct PDF geometry, and 3/3/6 clickable annotations. Preserved the original granular PowerPoint plus an editable HTML/CSS production source under `output/source/`. The webpage remained unchanged; no commit, push, PR, or deployment was performed. |
| RH-012 | Done | Rebuild the half-page invitation for print legibility and add a full-page letter insurance version. | Completed 2026-08-03 without changing the webpage or 5 × 7 invitation. Rebuilt the invitation as native, responsive 5.5 × 8.5 and 8.5 × 11 print masters rather than scaling the 5 × 7 artwork. Preserved the field-atlas design, content hierarchy, program structure, sponsors, partners, QR destination, visitor invitation, and accessibility contact while materially enlarging body copy, program details, event information, QR treatment, sponsor marks, partner marks, and footer text. Rebuilt the two-up 11 × 8.5 sheet from two exact 396 × 612 point panels meeting at the centerline with no white perimeter, gutter, or dashed cut guide. Added a new full-page 8.5 × 11 PDF. All three PDFs are one page at exact geometry and retain 3/6/3 clickable annotations; rendered proofs show no clipping or overflow. Editable HTML/CSS and PDF-build sources are preserved under `output/source/2026-rural-health-invitation-print/`. No commit, push, PR, or deployment was performed. |
| RH-013 | Done | Add Dogwood Health Trust to the print-optimized invitation sponsor row. | Completed 2026-08-03 across the native 5.5 × 8.5, two-up 11 × 8.5, and full-page 8.5 × 11 invitation PDFs. Used the supplied `src/assets/sponsors/dogwood.png` wordmark, placed it immediately right of Transylvania County Tourism Development Authority, and rebalanced the five-logo row around the page center in both print masters. Added a clickable Dogwood destination to `https://dogwoodhealthtrust.org`, producing 4/8/4 annotations. Rendered proofs show no clipping, overflow, gutter, or sponsor-row imbalance. The 5 × 7 invitation and tiered event-page sponsor display were not changed; Dogwood's website tier remains pending because no tier was supplied. No commit, push, PR, or deployment was performed. |
| RH-014 | Done | Add the new Foothills sponsor tier to the half-page invitation without changing its layout. | Completed 2026-08-06 for only the native 5.5 × 8.5 invitation and two-up 11 × 8.5 landscape sheet. Kept the four existing sponsor marks at primary prominence and grouped Dogwood Health Trust plus Vaya Health as a smaller Foothills pair after a subtle divider, without printing tier names or increasing the sponsor-row height. Archived the supplied simulator-project logo files with the editable print source, regenerated only the two requested PDFs, and confirmed the full-page 8.5 × 11 PDF checksum remained unchanged. Poppler proofs show no clipping, overflow, gutter, or movement of the visitor line/footer; exact geometry remains 396 × 612 and 792 × 612 points, with 4/8 clickable annotations. No commit, push, PR, or deployment was performed. |
| RH-015 | Done | Add a $40 Impact Health-supported Inclusive Registration rate alongside the existing $65 Standard rate on the Rural Health Convening page. | Merged via PR #120 (merge commit `16ddef2`) 2026-08-07, after confirming GitHub Actions runners had recovered from the outage that briefly held up the merge (verified with a clean rerun of a previously-failed deploy run before merging). Implementation: one existing Squarespace ticket product, shared inventory, and one-ticket limit are preserved — no second product or parallel registration flow. The $25 credit applies via a Squarespace discount code (`IMPACT-REG`, created by the owner) because Squarespace cannot pre-apply a code selectively per visitor choice on a shared product; the Inclusive card copies the code to clipboard and always shows it as persistent visible text, with a tooltip explaining the registration-form step that precedes the checkout promo-code field, and a stated partial-refund recovery path if the credit doesn't apply (confirmed operationally feasible against Squarespace's refund tooling). Impact Health is credited via a monochrome wordmark sized/aligned to match the event's own mark; copy explicitly signals the rate's purpose ("so cost isn't a barrier to attending") without application, explanation, or disclosure. Automated Codex PR review flagged that the copy-code button could navigate to checkout before confirming the clipboard write succeeded or failed, risking a registrant believing the code was copied when it wasn't; fixed by gating navigation on the clipboard promise settling, with a second click proceeding once the manual-copy fallback has been shown. Verified via `npm run build` (81 pages prerendered, 0 failed), `tsc --noEmit`, and in-browser checks at 375/970/1000/1280/1480px (no overflow, cards aligned) plus both clipboard success/failure paths. **Open follow-up, not addressed in this PR:** Vaya Health still appears elsewhere on this page as a general "Foothills" tier event sponsor (RH-014, immediately above) unrelated to this credit — owner to confirm whether that listing should also change now that Impact Health is the actual supporter of the inclusive rate. The matching print-invitation update is RH-016 (below), completed separately via Codex. | Resolve the Vaya Foothills-listing question. |
| RH-016 | Done | Integrate the approved Inclusive Registration information into the half-page and two-up print invitation without disturbing the established layout. | Completed 2026-08-06. Replaced only the small convening-name divider above the logistics block with a full-width, brass-ruled registration rail using the existing field-atlas palette and typography. Included the approved heading, body, `IMPACT-REG` code, checkout instruction, and one subtle single-color Impact Health mark; changed the half-page footer prompt to `Registration or accessibility questions?` so prohibited wording does not appear. The date/time/place block, primary ticket QR, sponsor row, visitor line, partner footer, and panel geometry retained their prior positions. Regenerated only the 396 × 612 point half-page and edge-to-edge 792 × 612 point two-up PDFs; verified exact copy in the HTML source, crisp OCR recognition of `IMPACT-REG`, 4/8 link annotations, no forbidden terms, no rail overflow, and an unchanged checksum for the separate full-page PDF. Follow-up 2026-08-07: replaced the time-sensitive `Registration is open / ONLY 200 SEATS` callout with the evergreen `Seating is limited / RESERVE YOUR SEAT`, preserving the existing two-line styling and positions; regenerated and reverified only the half-page and two-up outputs. No commit, push, PR, or deployment was performed. |
| RH-017 | Done | Turn off the public seats-remaining ticker on the Rural Health Convening page and add an unlisted internal status dashboard at `/internal/status` for the team (and, as scope grew, partners) to check it privately. | Merged via PR #122 and #123. The public ticker is gated by a single flag (`SHOW_RURAL_HEALTH_SEATS_TICKER` in `src/data/opsStatus.ts`, currently `false`) that hides both display spots on the convening page without touching the underlying fetch — one-line flip to re-enable. `/internal/status` is not linked anywhere, `noindex`, excluded from the sitemap/prerender by construction (nothing links to it for the crawler to discover), no site header/footer (added to `CHROMELESS_PATH_PREFIXES`/`CUSTOM_FOOTER_PATH_PREFIXES` in `src/App.tsx`), and shares the seats-fetch hook (extracted to `src/hooks/useRemainingSeats.ts`) with the public page. Redesigned in PR #123 to match the convening page's own design system (Fraunces/Manrope, forest/paper/clay/brass palette) and the same "SparkPoint, NCRHA & FHLI present [WNC event mark]" brand lockup used in that page's masthead, once the page's audience grew from purely internal to including partners. Content is a curated, presentable snapshot: days-to-event, the live seat count regardless of the public toggle, and the actual NCRHA Fall 2026 Convening Monday.com board (`https://yoursparkpoint-company.monday.com/boards/18414150575`) grouped by status rather than Monday's lane placement — name/status/due-date only; assignees, notes, files, and budget are excluded as not partner-appropriate. This is a manual snapshot, not a live sync (owner's call); re-sync by pulling the board again and updating `src/data/opsStatus.ts`. Verified via `npm run build` (81 pages prerendered, 0 failed each time) and in-browser checks at desktop/mobile widths, both times. | Re-sync the Monday snapshot periodically; revisit the "unlisted + noindex, no password gate" access model if partner sharing expands further. |
| RH-018 | Done | Add three new sponsors to the Rural Health Convening page: First Citizens Bank at Foothills, plus a new Friends tier holding AdventHealth and UnitedHealthcare. | Review and merge. | Branch `Rural_Health_sponsors_august`. Owner supplied three logo PNGs; each was alpha-trimmed and re-encoded to webp into `src/assets/sponsors/` (`first_citizens.webp` 340x160 lossy q92 since the mark carries gradients, `advent_health.webp` and `united_healthcare.webp` lossless). First Citizens appends to the existing `foothillsSponsors` array; a new `friendsSponsors` array and a matching `rh-friends-field` block add the fifth tier, rendered in both the hero credit strip and the partnership-section logo hierarchy, and all five new orgs (plus Vaya Health, which had been missing) were added to the page's `Event` structured-data `sponsor` list. **Sizing is per-logo, not per-tier:** AdventHealth stacks an icon over its wordmark and UnitedHealthcare sets its name on two lines, so both need a taller box than a single-line wordmark to read at the same optical weight — heights were set from measured ink coverage rather than tier rank, then tuned in-browser so Friends still reads a step below Foothills. **One incidental fix:** the hero tier separators were standalone `<span>` flex items, so with a fifth tier the strip wrapped to three lines and left a rule stranded at the end of a line with nothing after it (a leading-rule strand already occurred at 1280px before this change). Each separator is now a `::before` on the tier it precedes, so it can never wrap alone. Verified: `npm run build` (109 pages prerendered, 0 failed), `tsc --noEmit` clean on both touched files (the repo's pre-existing errors in `src/imports/*-backup.tsx` are untouched), and in-browser at 1280/760/375px — no horizontal overflow, no orphaned rules, tier hierarchy legible at every width. Owner confirmed 2026-08-17 that the AdventHealth sponsor is the corporate entity, so it is billed as "AdventHealth" linking to adventhealth.com; First Citizens Bank and UnitedHealthcare use their national sites for the same reason. No commit, push, PR, or deployment was performed. |
| EVENT-001 | Done | Add a reusable SparkPoint events hub at `/events` and publish the Rural Health Convening as its featured event. | Merged in PR #85 and verified live 2026-07-27 after GitHub Pages deployment run 30297118711. “Where We Gather” is driven by `src/data/events.ts`, automatically separates upcoming/past gatherings, and is integrated into navigation, footer, structured data, and prerender routes. |
| EVENT-003 | Done | Add a `/pcr_collab` branded vanity redirect to the existing `/events/thrive-at-five` page. | Opened in PR #124. Same pattern as `/sponsors`, `/commconn`, and the other internal aliases: a real static 200 stub generated at build time (`noindex`, canonical to the target, meta-refresh + JS `location.replace`) so a direct hit, QR scan, or crawler doesn't get a GitHub Pages 404 before the redirect runs (the SEO-001 class of bug), plus a client-side `<Navigate>` route for in-app navigation. Verified via `npm run build`: 82 pages prerendered (up from 81), 0 failed; confirmed the generated `build/pcr_collab.html` stub matches the established pattern exactly. | Merge PR #124. |
| REPORTS-001 | Done | Build a real, Supabase-auth-gated internal portal for monthly social media reports at `/internal/reports`, replacing a one-off static HTML dashboard with no update path. | Merged via PR #128. `/internal/login`, `/internal/reports`, and `/internal/reports/:reportType/:period` are excluded from static prerendering by the same mechanism as `/internal/status` (RH-017), so the Supabase Auth gate is enforced client-side rather than by hiding a public route. Data lives in a new `report_snapshots` table (RLS restricted to `authenticated`) plus an `internal-reports` storage bucket for PDF/DOCX briefs; refreshed monthly via `scripts/internal-reports/ingest.mjs`, which deliberately keeps CSV-parsing and the Supabase upsert as two separate stages so the CSV-pull step alone can be automated later (e.g. Meta Graph API) without touching how data lands in Supabase. The report dashboard went through several design rounds in-session (culminating in a scroll-driven visual narrative — hero number, three data-driven "beats," a momentum readout, a funder-ready summary paragraph) before landing on the version merged in PR #128; narrative/insight generation (`reportNarrative.ts`, `reportInsights.ts`, `trajectoryInsights.ts`, `reportBeats.ts`) is fully data-driven and generic across any month, with explicit honesty guards (no "steady growth" claim unless no single month is >60% of the total, no spin on a real decline). Post-merge, an automated Codex PR review on #128 flagged three real findings, fixed in a follow-up PR: (1) P1 — `InternalReportDashboardPage.tsx` only imported its own `internalReportDashboard.css`, not the shared `internalReports.css` that actually holds the `.ir-print-*` rules; with Vite's per-lazy-route CSS splitting, a direct/bookmarked visit to the dashboard (skipping the login/home chunk) would print with no styling — fixed by importing `internalReports.css` from the dashboard route too. (2) P1 — the three new routes weren't recorded in AGENTS.md's route inventory or here — fixed by this entry and the corresponding AGENTS.md update. (3) P2 — `ingest.mjs`'s date-merge loop initialized a new day's row with only `{ date }`, so a date present in one metric's CSV export but absent from another would upload with that field's key missing entirely (`JSON.stringify` drops `undefined`), and the dashboard's `.toLocaleString()` calls would throw on it — fixed by zero-initializing all six metric fields when a date row is first created. Verified via `npm run build` (82 pages prerendered, 0 failed, `/internal/*` confirmed absent from prerendered output) and `tsc --noEmit` clean on every touched file, both before and after the review-finding fixes. | Jeff to spot-check the fixed print/direct-visit path and the dashboard generally now that the structural clipping/reveal-timing issues found during review are resolved; upload the July results-brief PDF/DOCX to the `internal-reports` bucket if not already done; provision a Supabase Auth user per person who needs access (Dashboard → Authentication → Users → Invite user — no self-serve signup, no tooling access to automate this). |
| PROG-001 | Next | Distill SparkPoint's active programs to a fixed, named program list grouped under three pillars (Workplace, Youth, Community Connectors), each assigned a Listen/Learn/Lead pathway (retained, not replaced, from the existing model). | Owner-supplied 2026-07-31 target list with Listen/Learn/Lead assignments (Claude-drafted 2026-07-31, owner accepted as a working baseline — several are lower-confidence guesses to revisit as content firms up): **Workplace** — Connected Leadership (Learn, high confidence); Leadership Wellbeing (Learn, high confidence); SparkPurpose (Learn, high confidence). **Youth** — VOS (Lead, high confidence); Mentorship (Learn, medium confidence — needs branding); Family/Parent Program (Learn, low confidence — needs branding). **Community Connectors** — PowHer (Lead, low confidence); Story Collection (Listen, high confidence — branding to be revisited); Media Navigators (Listen, high confidence); Resource Lobby (Lead, medium confidence — physical presence at the Resilience Hub, plus a listing on the Resource webpage); Facilitation (Lead, high confidence — convening/organizing/coordinating: events, collaborations, group projects, external lectures, etc.); Simulator (Lead, low confidence — needs a full branding pass; covers the platform and facilitation of the first simulator, with an eye toward expanding into other community software experiments). High-confidence assignments matched directly to how similarly-named existing programs are already categorized in `src/pages/programs/programsData.ts`; low/medium-confidence ones are open to revision. Current site programs still use the old ~20-program list under Listen/Learn/Lead; reconciliation/rebuild still needed once branding is finalized. Related to EDIT-001 (editorial QA for Programs content) — confirm with owner whether EDIT-001 folds into this item once taxonomy work starts. |
| EDIT-001 | Next | Complete editorial QA for Programs content. | Group with program-content updates only. |
| EDIT-002 | Next | Curate fuller News/Media archive. | Group releases, media kits, and News/Media card updates. |
| UX-001 | Next | Polish sponsor and resilience hub content/assets. | Separate content/design bundle. |
| PERF-001 | In progress | Mobile performance (baseline Lighthouse 77). | Merged: PR #75 (compress textures + defer homepage infographic) and PR #76 (self-host Manrope via @fontsource, removing render-blocking Google Fonts + more image squeeze). Prod 77->80 after #75; #76 now live (expect higher). NEXT: re-run production PageSpeed; Tier 3 = Cloudflare edge (cache lifetimes + security headers, pairs with SEC-005) for 90+. Follow-ups: CLS 0->0.021 recheck, a11y contrast 96->100. | Re-run PageSpeed; green after Tier 3. |
| STORY-001 | Done | Rebuild /stories as the "Story Hub" (featured, filter+search two-column feed with type badges, live impact counter from impact2025.ts, Behind the Stories, Echoes strand, new Programs category + Community Connectors story). | Merged and deployed. | Story Hub live at /stories; legacy stacked layout replaced. |
| STORY-002 | Deferred | Community "where stories come from" map for the Story Hub. | Use a REAL area basemap (accurate WNC/county map) + a full CODEX visual-element creation run. Pins are community-level aggregate counts only, never tied to a specific story (anonymity). | Polished map section shipped into the Story Hub. |
| STORY-003 | Done | Behind the Stories reimagined as a folder-driven cinematic photo-shuffle reel (Embla) + Echoes cover-art fix. | Merged PR #71 (2026-07-22). Drop images in public/assets/behind_story to update (natural filename sort). | Shipped on /stories. |
| TICKET-001 | In progress | Branded ticket checkout: `secure.yoursparkpoint.org` set as the Squarespace commerce site's primary domain (store + checkout on-brand); main site adds `/tickets` and `/tickets/:slug` vanity redirects. | Slug→URL map in `src/data/ticketLinks.ts`; first event `wncrrhc` (WNC Regional Rural Health Convening). 2026-07-30 follow-up merged via PR #95: added the print-friendly `/rh_tickets` alias in the React router and prerender redirect map, targeting the direct Rural Health Convening purchase page. Vite production compilation and `git diff --check` passed; a real-browser check reached the exact secure purchase URL with the correct product title and no console errors. Squarespace-side DNS/SSL done by Jeff. REMAINING (per `.storyhub/branded-ticket-checkout-HANDOFF.md`): disconnect leftover `tickets.yoursparkpoint.org` subdomain — still live/resolving as of 2026-07-26, not yet cleaned up; repoint `/donations` and `/newsletter` (`src/App.tsx`) at `secure.yoursparkpoint.org/...` — still hitting raw `cowbell-primrose-tet2.squarespace.com` as of 2026-07-26. | `/tickets`, `/tickets/wncrrhc`, and `/rh_tickets` resolve to secure.yoursparkpoint.org. |
| HOME-001 | Done | Redesign the homepage `#impact` section's "Community Built in Transylvania County" diagram and "Connection is a Team Effort" partner hub — merged into one section. | Background "floating in space" fix: topo-contour texture + layered ridgeline silhouettes + radial vignette + two slow "signal ping" points on the outer ring. `/sponsors` renamed to `/partners` to match the nav label (redirect, SEO, prerender STATIC/SKIP, sitemap all updated and verified; hardened further in PR #96 follow-up to emit a real static redirect stub instead of relying on the SPA fallback). New canonical `src/data/partners.ts` (6 sectors, real `url`/`logoKey` only for the 8 confirmed orgs, nothing fabricated) now powers BOTH the homepage diagram and `/partners` — the two lists' spelling/color/icon drift is resolved. `PartnerNetworkHub.tsx` deleted; its sector-node interaction folded into the map diagram via a new `CommunityNetworkDiagram.tsx`: desktop shows 6 corner nodes around the map, mobile shows a compact chip row instead of a second radial layout (cuts mobile height — the old ~900px standalone hub is now a ~76px row); tapping a sector opens a compact anchored `Popover` (replacing an initial `Sheet` after owner feedback that it felt clunky) listing partners with real links first, plus a link to the full `/partners` directory. WNC outer ring is now tappable too (reveals a stewardship line). Verified end-to-end via DOM/functional checks (screenshot capture was unreliable this session due to an environment glitch, confirmed unrelated to the code): 6 sector nodes + mobile chips render, Popover opens with correct sector data and real links (e.g. UNC Health Pardee), `/partners` renders all 6 cards/icons/8 logos and its own sector modal now shows real links (e.g. Comporium, Self-Help Credit Union) where confirmed, `/sponsors` redirect still works, no console errors beyond a pre-existing unrelated Hero.tsx warning. | Live and verified 2026-07-30; open in PR #96, review/merge remain. |
| DATA-001 | Next | Single source of truth for partner, Need Help/resource, and impact data — today split across three independently-maintained, drifting data shapes (`PartnerNetworkHub.tsx`'s flat partner list, `src/data/resources.ts`'s typed `ResourceEntry`, `src/data/impact2025.ts`'s hand-edited stats). Goal: one editable store (Supabase is already wired into this project server-side — vs. introducing Monday.com as a second, syncing source of truth. **Corrected 2026-08-11:** this previously cited `kv_store` as the existing mechanism. That was never the live one — the deployed `make-server-393f2b0a` function writes to a real `intake_submissions` Postgres table, and the only `kv_store` in the repo lived in the shadow edge-function copy removed in PR #130. This is better news for DATA-001 than the original note implied: the starting point is a relational table, not a key-value store, so a schema can be designed directly rather than migrated off KV) with a single-entry form + batch JSON import/export, so Charlotte, Maggie, Jenny, and Sarah can log events/programs without a code deploy. | Explicitly deferred to a dedicated working session (owner wants to think through what impact data to collect at which kinds of events before designing the schema). Also raised: cost concern about paying for "another separate database" — worth clarifying in that session that Supabase is already provisioned for this project, not a new subscription. **Punch list — owner-verified partner links ready to import when this session happens** (currently NOT in `src/data/partners.ts`, held here per owner request rather than hand-edited in): Rise & Shine Afterschool Program — https://www.riseshine.org/; Through the Trees — https://www.throughthetrees.us/; Meals on Wheels (Brevard) — https://www.mealsonwheelsbrevard.org/; Jameson's Joy — https://www.facebook.com/jamesonsjoy/ (Facebook page, no standalone site); Looking Glass, CPA — https://www.lookingglasscpa.com/; FWRD Transylvania (LTRG) — https://gofwrd.org (supersedes the dead fwrdtransylvania.org found 2026-07-30); Center for Trauma Resilient Communities — https://traumaresilient.org (supersedes the dead ctrcteam.org); Town of Rosman — https://www.townofrosman.com (supersedes the dead rosmannc.com); Alulua — https://alulauplift.org (supersedes the dead alulua.org). | Schema proposal + collection-form plan reviewed and agreed before any build starts. |

## Reference material

- [Security audit report](security_best_practices_report.md) — 2026-07-17 baseline and evidence.
- `AGENTS.md` — repository architecture and working rules. It points here for the live work queue.

## Change log

| Date | Update |
| --- | --- |
| 2026-08-17 | Opened RH-018 on branch `Rural_Health_sponsors_august`: added First Citizens Bank to the Foothills tier and created a Friends tier for AdventHealth and UnitedHealthcare on the Rural Health Convening page, in both the hero credit strip and the partnership-section hierarchy, with the logos trimmed and converted to webp and the structured-data sponsor list brought up to date (Vaya Health had been missing from it). Also converted the hero tier separators from standalone spans to `::before` pseudo-elements so the now-three-line strip cannot strand a rule at the end of a wrapped line. Build green at 109 pages, 0 failed. Owner confirmed the sponsor is the corporate AdventHealth entity (not AdventHealth Hendersonville), so it is billed as "AdventHealth" linking to adventhealth.com. |
| 2026-08-13 | Opened PR for SEO-003: consolidated six more legacy Squarespace URLs into real redirect stubs, made `/stories/community-champions/helene-anniversary` canonical for the Helene one-year story (it previously had no build output at all, so SEO-001's redirect pointed at a 404), and removed the `noindex` that was cancelling those stubs' canonical consolidation. Also fixed both `vite.config.mts` build defaults, which had been making local builds emit empty prerendered shells and `www.` canonicals that production never ships; `prerender.mjs` now fails loudly instead of silently writing them. Logged SEO-004 for three legacy URLs that need owner knowledge. |
| 2026-08-10 | Fixed three findings from an automated Codex review on merged PR #128 (REPORTS-001): the report dashboard route wasn't importing the shared stylesheet holding the actual print rules (would print blank on a direct/bookmarked visit); the three new `/internal/*` routes weren't documented in AGENTS.md or here; and `ingest.mjs`'s date-merge loop could upload a `daily_metrics` row with a metric field missing entirely (not just zero) if that date was absent from one CSV but present in another. |
| 2026-08-09 | Merged PR #128 (REPORTS-001): shipped the internal social media reports portal at `/internal/reports`, real Supabase-auth-gated (not obscurity), with a scroll-driven visual-narrative report dashboard and a monthly CSV-to-Supabase ingest script. |
| 2026-08-07 | Refined RH-016 invitation language: replaced the fixed 200-seat callout with the evergreen `Seating is limited / RESERVE YOUR SEAT`; regenerated and visually verified only the half-page and two-up PDFs while preserving geometry, annotations, and the untouched full-page checksum. |
| 2026-08-07 | Opened PR #124 (EVENT-003): added a `/pcr_collab` branded redirect to `/events/thrive-at-five`, following the existing internal-alias pattern (static 200 stub + client-side route). Documented in AGENTS.md's route inventory and here after a Codex review finding flagged both as missing. |
| 2026-08-07 | Completed RH-017 (PR #122, #123): turned off the public Rural Health Convening seats ticker behind a one-line flag, and shipped `/internal/status` — an unlisted, noindexed status dashboard with the live seat count, days-to-event, and the actual event-planning Monday board. Redesigned to match the convening page's own look and added the SparkPoint/NCRHA/FHLI presenting lockup once the page started being shared with partners. |
| 2026-08-07 | Marked RH-015 Done: PR #120 merged (commit `16ddef2`) after confirming GitHub Actions runners had recovered. Corrected a Codex review finding that the RH-015 entry still read "In progress" with a stale "merge blocked" next-action after the merge landed; also corrected its follow-up note, which still described RH-016's print-invitation update as "being scoped" after RH-016 had already completed. |
| 2026-08-06 | Completed RH-016: integrated the approved Inclusive Registration copy, `IMPACT-REG` code, and subtle Impact Health mark into a full-width brass-ruled rail on the half-page and two-up invitation; verified exact geometry, copy, links, rendered layout, prohibited-language absence, and that the full-page PDF remained untouched. |
| 2026-08-06 | Opened PR #120 (RH-015): added a $40 Impact Health-supported Inclusive Registration rate alongside the existing $65 Standard rate on the Rural Health Convening page, using the existing ticket product/inventory/checkout flow and a Squarespace discount code (`IMPACT-REG`). Fixed a Codex-flagged review finding where the copy-code button could navigate to checkout before the clipboard write settled; navigation is now gated on that result. Merge blocked only on the current GitHub Actions outage. |
| 2026-08-06 | Completed RH-014: added Dogwood Health Trust and Vaya Health as a compact, lower-prominence Foothills pair in the existing half-page sponsor row; regenerated only the 5.5 × 8.5 and two-up 11 × 8.5 PDFs; and verified layout stability, exact geometry, link annotations, and that the full-page PDF was untouched. |
| 2026-08-03 | Completed RH-013: added Dogwood Health Trust immediately right of the TDA mark in the print-optimized half-page and full-page invitation masters, recentered the five-sponsor row, added the supplied Dogwood URL as a clickable PDF annotation, and regenerated the half-page, two-up, and letter PDFs. The tiered webpage sponsor display remains unchanged pending Dogwood's tier. |
| 2026-08-03 | Completed RH-012: rebuilt the half-page invitation as a print-legibility master with larger body/program/event text, QR treatment, sponsor and partner marks, and contact information; rebuilt the two-up sheet with two edge-to-edge native 5.5 × 8.5 panels and no gutter; and added a native full-page 8.5 × 11 insurance version. Verified exact geometry, rendered layout, and 3/6/3 clickable annotations. The 5 × 7 invitation and event webpage were intentionally unchanged. |
| 2026-08-03 | Completed RH-011: corrected the RH-010 export regression by restoring the invitation's full Fraunces/Manrope design, replacing the program section as one clean layer with Jeff's final copy, resolving spacing and clipping, rebuilding the sponsor row without tier names, and consolidating to one dominant event-details-and-tickets QR pointing to the convening page. Regenerated and verified the PPTX, PNG, 5 × 7, half-page, and two-up print files; preserved original and HTML/CSS editing sources. |
| 2026-08-03 | Completed RH-010: refined the invitation in place, preserving its typography and layout system while removing sponsor tier labels, adding Impact Health to Laurie Stradley's attribution, pluralizing the program overview language, and distinguishing a secondary event-details link from the primary registration QR/short URL. Regenerated and visually verified all editable and print formats; the convening webpage was intentionally unchanged. |
| 2026-08-03 | Completed RH-009: produced a 5.5 × 8.5 half-page invitation PDF and a two-up 11 × 8.5 landscape letter print sheet from the current July 31 official invitation. Preserved proportions and all current program, registration, sponsor, partner, visitor, and accessibility content; verified exact page geometry, two-copy text duplication, and final render quality. |
| 2026-08-01 | PROG-001 branding pass round 1: **Simulator** category confirmed as **Signal Lab** (owner: "love it, lots of graphic branding potential"), branded as a platform/capability rather than a single app — Rural Health Field Simulator is its first flagship instance. **Mentorship** working name reverted from a Claude-drafted "Peer Mentor Corps" to plain "Mentorship" — owner flagged "Corps" as politically loaded from multiple directions right now, and that a youth peer-mentoring program's name should come from the peer mentors themselves once the program is running, not be imposed top-down. Mentorship's "why it exists" framing corrected: SparkPoint is a convener/connector (resilience-based training & support), not a suicide-awareness/intervention org — that lane belongs to partner org TC Strong, who should be named as the specialized resource mentors are trained to route to. **Family/Parent Program** confirmed as a resource-navigation model (warm handoffs, a place to connect, a place to learn); a future support-group layer is possible but not now, and the program is in a holding pattern pending Resilience Hub funding. Noted for later Facilitation branding: Dr. Ora's (SparkPoint board president) Brain Health Speech is a concrete real example of the Facilitation program in action. Noted for later StoryLab/Media Navigators context: the Resilience Hub build includes a tight 2-person StoryLab studio + editing bay; Media Navigators is the fellowship feeding into it as it grows. Story Collection branding remains explicitly deferred/not a priority. |
| 2026-07-31 | PROG-001: drafted a Listen/Learn/Lead pathway assignment for all 12 distilled programs (7 high-confidence direct matches to existing categorized programs; Mentorship, Family/Parent Program, PowHer, Resource Lobby, and Simulator are lower-confidence guesses). Owner accepted the full draft as a working baseline to move forward with, to be revised as branding/content firms up rather than perfected now. |
| 2026-07-31 | Opened PROG-001: owner supplied a distilled target list for SparkPoint's active programs, grouped under three pillars (Workplace / Youth / Community Connectors). Several entries (Mentorship, Family/Parent Program, Simulator, Story Collection) are flagged as still needing a branding pass. No code changed yet — logged as upcoming work. |
| 2026-07-31 | Clarified PROG-001: the existing Listen/Learn/Lead pathway model in `programsData.ts` is retained, not replaced, by the Workplace/Youth/Community Connectors grouping — each distilled program will still need a Listen/Learn/Lead assignment, TBD pending owner input. |
| 2026-07-31 | Completed RH-008: reworked the convening hero's sponsor/partner display into one consolidated, linked logo element under the map figure (replacing a duplicate text-credit + logo-row pair), added NCRHA/FHLI as a delineated "In partnership with" group beside the funding sponsors, enlarged and repositioned the block to align with the left column, equalized the two partner logos' size, and linked NCRHA/FHLI everywhere they appear on the page. Verified via dev-server DOM/accessibility-tree checks; no console errors. |
| 2026-07-31 | Opened PR #99 (RH-007): expanded the October 1 program lineup from three generic cues to four named segments (statewide NCDHHS briefing from Maggie Sauer, Laurie Stradley's NC ROOTS update, a new story-collection segment, and the Field Simulator) on both the webpage and official invitation, and replaced the standalone regional-update spotlight with a general "historic moment for rural health in North Carolina" framing panel. The underlying lineup was directed by Jeff Bannister in-session; display label wording remains pending his final sign-off. Addressed a Codex review flag by recording that confirmation here, since RH-005's prior text (limiting confirmed cues to the simulator/cross-sector/shared-learning trio) was now stale. |
| 2026-07-30 | Completed RH-006 locally: removed the temporary secondary Program Preview/agenda card and placeholder timing from Laurie Stradley’s regional-update section, leaving the approved featured blurb in a single-column layout until confirmed program specifics are available. Desktop/mobile browser QA, Vite compilation, and diff checks passed; the invitation was intentionally unchanged. |
| 2026-07-30 | Opened draft PR #97 for RH-003 through RH-005. The branch includes Laurie Stradley’s featured Region 1 / NC ROOTS update, the broader simulator/cross-sector/shared-learning cues, the Explore Brevard visitor invitation, and the updated first simulator screenshot. Merged current `main`, resolved the worklist-only conflict while preserving PR #96 history, and reran Vite compilation and diff checks successfully. Review, CI, merge, and deployment remain. |
| 2026-07-30 | Completed RH-004 locally: added the supplied 1896 × 1642 `simulator_screencap.webp`, updated the first simulator-gallery reference, and preserved its existing alt text and responsive treatment. Desktop/mobile rendering and Vite production compilation passed. |
| 2026-07-30 | Completed RH-005 locally: added confirmed high-level experience cues and a restrained Explore Brevard visitor invitation to the webpage, official invitation, editable source, PDF, and email copy. Kept Laurie’s green feature card exclusive to her update; “What You’ll Experience” is a separate paper-colored program strip. Desktop/mobile browser QA, simulator-anchor interaction, Vite compilation, PPTX overflow, and 5 × 7 print checks passed. Added RH-004 as the separate follow-up to replace the first simulator gallery image with the supplied matching 1896 × 1642 asset. |
| 2026-07-30 | Completed RH-003 locally: added Laurie Stradley’s approved featured Region 1 / NC ROOTS update to the convening page, program preview, 5 × 7 official invitation, editable invitation source, and accessible email copy. Desktop/mobile browser QA, invitation rendering, PDF geometry, editable-source overflow, and Vite compilation passed. Exact agenda time, duration, slide needs, and technical support remain pending; no commit, PR, or deployment was requested. |
| 2026-07-30 | Added `/rh_tickets` for the Rural Health Convening invitation, merged via PR #95. It is wired as both a React external redirect and a QR-safe static prerender redirect to the exact secure purchase page; Vite compilation, diff checks, and real-browser destination verification passed. |
| 2026-07-30 | Completed RH-002, merged via PR #95: added Transylvania County Tourism Development Authority as the Highlands ($1,500) convening sponsor across the webpage, Event structured data, official 5 × 7 invitation, and email copy. Verified the sponsor hierarchy on desktop/mobile, the live purchase URL, Vite production compilation, and the rendered PDF. |
| 2026-07-29 | Opened HOME-001 (homepage `#impact` diagram redesign) and DATA-001 (unified partner/resource/impact data source). "Community Built in Transylvania County" now uses the real county map image with an interactive Listen/Learn/Lead cycle; next up is a background treatment so it doesn't read as floating, plus visual mockups for the "Connection is a Team Effort" partner-hub redesign. DATA-001 explicitly deferred to a dedicated session per owner request. |
| 2026-07-30 | HOME-001 closed: the two homepage partner/map sections are now one merged, shorter section, backed by a new shared `src/data/partners.ts` that also powers the renamed `/partners` page (formerly `/sponsors`, now redirects). Background "floating" complaint fixed with ridgelines/vignette/ring pings; sector nodes now open a real-links partner Sheet instead of a dead-end name list. |
| 2026-07-30 | HOME-001 follow-up: replaced the sector "pop-up" `Sheet` (felt like a full-screen lower-third takeover) with a compact, anchored Radix `Popover` (~288px card next to the tapped node/chip, dismissible by outside click/Escape) on both the homepage diagram and confirmed working on mobile. Owner supplied a verified-links research file (`sparkpoint_organization_links_2026-07-30.json`, 51 orgs with graded confidence: verified/probable/parent_site/social_only/social_or_informal/needs_review); live-checked all 14 "probable" URLs before publishing — 4 domains (fwrdtransylvania.org, ctrcteam.org, rosmannc.com, alulua.org) failed DNS resolution entirely and were NOT added (flagged back to owner). Added confirmed real links for 40 of 51 orgs to `src/data/partners.ts`; 12 partners remain intentionally unlinked (needs_review/social-only per the file, or failed live-check) pending owner research. |
| 2026-07-30 | Owner personally verified working replacement URLs for 9 of the still-unlinked orgs (including corrected domains for the 4 that failed DNS above). Per owner request, held these OUT of `src/data/partners.ts` for now — recorded as a DATA-001 punch list instead, to be imported when the dedicated data-source session happens rather than hand-edited piecemeal. |
| 2026-07-30 | Opened PR #96 (homepage partner-section merge + RH-002); addressed automated Codex review feedback (`/sponsors` now emits a real static prerender redirect stub, `AGENTS.md` documents `/partners` as canonical); merged latest `main` (PR #94/#95, which independently shipped the same sponsor-tier and RH-002 work) to resolve the resulting conflict. |
| 2026-07-27 | EVENT-002 built locally on `codex/thrive-at-five-coffee-release`: Thrive @ Five is now the featured gathering on `/events`, with a dedicated Common Ground Resilience Roast fundraiser page at `/events/thrive-at-five`, including an August 1 purchase CTA for the coffee; PR/deployment remain. |
| 2026-07-27 | PR #85 merged and deployed: `/events` and `/rural-health-convening` are live. Production verification confirmed HTTP 200 responses, correct priority-list links and 200-seat language, the Events navigation placement, the simulator section, and no relevant browser console errors. |
| 2026-07-21 | SEC-002 closed: `Protect main (production)` branch ruleset enabled (required PRs + build check, force-push/deletions blocked). |
| 2026-07-21 | SEC-003 closed: GitHub secret scanning and push protection enabled. |
| 2026-07-17 | Created as the canonical worklist; seeded with the security-audit remediation bundles and established project follow-ons. |
| 2026-07-17 | Deployed intake validation and a request-size limit; an invalid production submission was rejected with a 400 response before any stored or emailed side effect. |
| 2026-07-17 | Pinned every existing GitHub-owned action in the Pages deployment workflow to a reviewed full commit SHA, with release-version comments. GitHub Actions allowlist, Dependabot configuration, and a successful deployment remain required to close SEC-006. |
| 2026-07-21 | Story Hub (STORY-001) merged and deployed: /stories rebuilt as a featured + filter/search two-column feed with type badges, a live impact counter (impact2025.ts), Behind the Stories, an Echoes series strand, and a new Programs category holding the Community Connectors story. Follow-ons opened: STORY-002 (community map, deferred) and STORY-003 (Behind the Stories redesign). |
| 2026-07-22 | Shipped: Story Hub reel (#71), Privacy redesign (#74, SEC-008 page half), Perf Tier 1+2 (#75) and self-host fonts + image squeeze (#76). SEC-001 closed (AAAA + Pages DNS check green); CNAME persistence PR #77 deploying. SEC-002/SEC-003 confirmed enabled. |
| 2026-07-22 | Branded ticket checkout: `secure.yoursparkpoint.org` is the Squarespace commerce site's primary domain, so store + checkout are branded (`secure.yoursparkpoint.org/checkout`). Main site adds `/tickets` + `/tickets/:slug` vanity redirects (slug→URL map in `src/data/ticketLinks.ts`). Tickets sold as Squarespace Service products. Subdomain/DNS/SSL done in Squarespace by Jeff. |
| 2026-07-26 | GitHub Pages custom domain flagged "DNS Check In Progress" again after being fully green since 2026-07-22/23. `gh api repos/ChFxPro/sparkpointv15/pages` showed `https_certificate.state: "dns_changed"` ("Detected a change to DNS settings. Requesting a new certificate.") — a benign automatic recheck, not an outage: apex kept serving `https://yoursparkpoint.org` at 200 with the prior cert (valid to 2026-09-13) throughout. No repo/workflow changes correlate (git log clean since `fa8f9bf`). Most likely trigger: Squarespace-side domain-panel activity tied to TICKET-001 (`secure.yoursparkpoint.org` connect + still-pending `tickets.yoursparkpoint.org` disconnect) — Squarespace republishes the full DNS zone on subdomain connect/disconnect, which GitHub picks up as a change on the apex/www records even though TICKET-001's subdomain is otherwise isolated from the apex A records. No DNS/Pages settings changed in response; expected to self-clear (GitHub SLA up to 24h). If still stuck, the documented fallback is Settings → Pages → remove custom domain → save → re-add `yoursparkpoint.org` → save (see `DNS-SSL-DIAGNOSIS-SPARKPOINT.md`). |
| 2026-08-02 | Opened SEO-001: Jeff shared a fresh GSC "Page indexing" export (73 not-indexed / 37 indexed). Cross-referenced it against a live, logged-in Search Console session and the codebase — root cause was `scripts/prerender.mjs` never generating static redirect stubs for most retired alias routes (`/news-media`, `/newsroom`, `/commconn`, `/resiliency-hub`, `/volunteer`, `/partner`, `/contact`, `/healthcare-story`), so GitHub Pages served a real 404 before the client-side redirect ever ran — the same class of bug already fixed for `/sponsors` in PR #96. Also found `/donations`/`/newsletter` still pointed the prerender map at the old raw Squarespace host after App.tsx was repointed to `secure.yoursparkpoint.org` (commit 2f0b17f). Live-verified via GSC's per-issue URL tables and URL Inspection's "Test live URL" that the bulk of the reported 404s are actually dead pre-migration Squarespace URLs (expected, not fixable) and several "Discovered"/"Crawled" entries were stale `www` crawl data or `www` sitemap duplicates. Removed a stale `www.yoursparkpoint.org/sitemap.xml` submission in Search Console (submitted back in March, still being re-read) that was feeding that duplicate-discovery noise. See SEO-001 for full detail. |
| 2026-08-02 | Completed SEO-002: addressed two further GSC findings via an orchestrated multi-agent workflow (separate isolated agents for cross-linking and directory titles, merged and build-verified together before shipping). Added related-content cross-links to story, program, and resource-directory pages so deep content pages — which GSC showed getting almost no internal links — now link to other relevant same-type pages. Added a `seoTitle` field to Resource Directory entries in `src/data/resources.ts` so partner-organization pages can carry a title matching real search intent, addressing GSC data showing exact-name searches for partner orgs getting impressions but zero clicks. Verified with a full production build: 100 pages prerendered, 0 failed. |
| 2026-08-11 | Removed the repository's shadow edge-function copy (PR #130). `src/supabase/functions/server/` — the original Figma Make scaffold layout — held a second, diverged implementation of the intake handler (527 lines vs. 368 in the deployed one). It was costing double maintenance: SEC-004's 16 KB body limit and field validation had to be written into both files. Verified dead before removing: nothing imports it, Vite never bundled it, the Supabase CLI only reads `supabase/functions/`, and `supabase functions list` shows no `make-server-535d8907`, so its `/impact/init`, `/impact/metrics` and `/volunteer` routes were never live endpoints. The deployed function named `server` belongs to a different repo (`DataAdmin/Spwebdatahandlingapp`, same project ref) — removing this also closes the footgun where a deploy from the wrong functions root would have overwritten it. Its one piece of unique data, the 2024 impact totals, is preserved as `src/data/impact2024.ts` in a separate first commit so the removal can be reverted without losing them; `averageAttendance` is kept as authored (72.1) with its discrepancy against 7000/97 documented inline rather than silently recomputed. Corrected DATA-001's `kv_store` premise above. `AGENTS.md` now records where edge functions actually live; `docs/metrics/metrics-inventory.md` got a supersession note rather than an edit, and the dated recon docs were left untouched. |
| 2026-08-11 | Completed the missing half of SEC-002 and advanced SEC-006 (PR #131). **SEC-002 was already Done and stays Done** — the `Protect main (production)` ruleset has blocked deletions, force-pushes and direct pushes since 2026-07-21. What its 2026-07-21 note overstated was "passing build required": the ruleset had no `required_status_checks` rule, because no CI existed to require. That is now true rather than aspirational. (Worth recording for the next person: `GET /repos/:o/:r/branches/main/protection` returns **404 Branch not protected** on this repo even while the ruleset is actively enforcing — classic branch protection and rulesets are different APIs. Check `GET /repos/:o/:r/rules/branches/main` for what is actually in force.) Added the repo's first PR gate: `ci.yml` (mirrors deploy.yml's build exactly, so a green check predicts the deploy) and `gitleaks.yml` (full-history secret scan). Deliberately no typecheck/test step — there is no `test` script and `tsc` does not pass on the Figma Make versioned import specifiers; a gate that is red on day one gets ignored. Full history scanned: 250 commits, 4.93 MB, two findings, both the same false positive — the Supabase `publicAnonKey` (decoded claim `role: anon`, public by design) in the autogenerated `src/utils/supabase/info.tsx`. **No real credential has ever been committed to this repo.** Allowlisted with its residual risk documented; verified the allowlist did not neuter the ruleset by confirming a planted GitHub PAT and RSA private key still fire. Per Codex review, pinned all three added actions to the reviewed full SHAs already vetted here on 2026-07-17 (re-verified against their tags), and additionally pinned the gitleaks tarball to its SHA-256 — the version pin alone would still have run unreviewed third-party code inside the secret scan. `build` + `gitleaks` were then added as `required_status_checks` **on the existing ruleset**, keeping one enforcement mechanism rather than two. (A classic branch-protection rule was briefly created here in error, on the mistaken reading of that 404 above, and removed once the ruleset was found — the ruleset was never edited except to add the checks, and its PR/force-push/deletion rules and empty bypass list are unchanged.) Rules now in force on `main`: `deletion`, `non_fast_forward`, `pull_request` (0 approvals, no bypass actors), `required_status_checks` → build, gitleaks. SEC-006's remaining pieces (Actions allowlist, Dependabot config) are still open; Dependabot itself reports 0 open alerts. |
| 2026-08-12 | Recovered the project's sourceless edge functions and decommissioned the 2026-07-31 triage tool (PR #132). **Four live functions on `suqtfbculwuetfdhdgdh` had no source anywhere on the machine** — they had been deployed by passing code inline to an MCP `deploy_edge_function` call, so the copy running on Supabase was the only one in existence: no history, no diff, no backup, and outside the build check and secret scan added in #131. All four were pulled down with `supabase functions download`. **Committed:** `rh-ticket-stock` (in production — the convening page's seat counter calls it via `src/hooks/useRemainingSeats.ts`; read-only, no secrets, fails closed to 503) and a canonical `supabase/functions/README.md`. **Deleted from the project instead of committed:** `program-triage`, `triage-api`, `probe-headers` — a program-triage tool built and self-tested 2026-07-31 and never rolled out (its submissions table held exactly one row, the owner's own). The first two hardcoded a shared dashboard key gating an endpoint that returned every submission, so they were deliberately never committed to this public repo; sources are backed up outside any repo at `~/Claude/backups/supabase-suqtfbculwuetfdhdgdh-20260812/`, and the key is inert now the functions are gone. Its one durable asset — the inventory of all 24 programs with path, format, cluster, listing source and an evidence assessment — was extracted to the Digital Brain vault (`08-Sources/sparkpoint-program-inventory-2026.md`) before deletion. **Verification:** every recovered file read in full before commit; `gitleaks` clean on the committed set with the key literal confirmed absent; build green. Also reconciled the now-stale "deploys exactly one function" claim in `AGENTS.md`, which this recovery invalidated. |
| 2026-08-12 | **Open hazard, not fixed: three stale deploy paths for `make-server-393f2b0a` would silently roll back SEC-004.** The live public intake function exists in four CLI-valid deploy directories. Only `sparkpointv15/supabase/functions/make-server-393f2b0a/` (11,808 bytes) carries the SEC-004 hardening; the other three are 7,461 bytes and were checked directly for `MAX_FORM_REQUEST_BYTES`/`readLimitedRequestBody` — **none has it**. Running `supabase functions deploy make-server-393f2b0a` from any of them would remove the 16 KB body ceiling and field validation from a live public form, with no error. Paths: `sparkpointv15/.claude/worktrees/resource-directory/`, `sparkpointv15/.claude/worktrees/mystifying-euler-52c838/`, and `dr_ora/.worktrees/sparkpoint-kyn/` — the last being a sparkpoint worktree parked under an unrelated repo, where nothing about the path suggests it can overwrite this project's production endpoint. Fix is to delete the three worktree copies (one is `git worktree` locked). Separately: `make-server-de2b7016` remains live with no local source — an orphaned Figma-Make auto-deploy superseded by `server`, deletable once confirmed nothing calls it. |
| 2026-08-12 | Closed the stale-deploy-path hazard and added guards so it cannot recur silently (PR #133). **Root cause:** `supabase functions deploy <slug>` reads `<cwd>/supabase/functions/<slug>/`, and nothing marks a directory as canonical — so every full checkout, including every git worktree, is a live deploy button. A machine-wide audit found **seven** CLI-valid deploy directories for `make-server-393f2b0a`, not the four first reported (the earlier search missed `~/.claude/jobs/`); three held a pre-SEC-004 copy at 7,461 bytes against the real 11,808 and would have silently stripped the 16 KB request ceiling and field validation from the live public intake form. **Fixed:** all six non-canonical checkouts removed, leaving exactly one deploy path, after confirming nothing would be lost — `origin/feat/resource-directory` (one unmerged commit) and `origin/feature/know-your-numbers-resource` were verified present on origin first, and a local-only `.gitignore` rule guarding `/public/databases/` against committing internal staff notes was ported here rather than discarded (nothing has ever been committed there, so it is preventive). Confirmed what is deployed is byte-identical to the repo copy. **Added:** `scripts/deploy-function.sh`, which refuses to deploy unless you are in the canonical checkout (not a linked worktree — the check that catches the actual failure mode), at the repo root, on `main`, level with `origin/main`, with the function's source committed, then requires confirmation; and `scripts/check-function-drift.sh`, which byte-compares every deployed function against the repo. Both tested: the guard was confirmed to refuse from a worktree (exit 1), and the drift check reports all deployed functions matching (`server` and `make-server-de2b7016` are declared as knowingly sourced elsewhere so the check passes green and any *new* sourceless function is a real signal). **Method note worth keeping:** a grep for `MAX_FORM_REQUEST_BYTES` reported the *correct* intake handler as unhardened, because the deploy-path version names it `MAX_INTAKE_REQUEST_BYTES` — `MAX_FORM_…` belonged to the shadow copy deleted in #130. Symbol-name checks are unreliable across variants; the byte comparison against what Supabase actually serves is not. |
| 2026-08-12 | Hardened the deploy guard and drift check per Codex review on PR #133 — three real gaps, all confirmed by test rather than inspection. **(1) P1, incomplete gate:** `deploy-function.sh`'s clean-tree check was scoped to the slug directory only, so an uncommitted change to the shared `supabase/functions/deno.json` — which Deno applies when bundling whichever function is being built — would have reached production through a slug directory that looked clean, bypassing the PR gate the script exists to enforce. Now checks every deployment input (`deno.json`, plus `deno.lock`/`import_map.json` if present); verified that `git status` over slug-plus-shared reports a dirty `deno.json` where the old slug-only form returned empty. **(2) P2, false green:** `check-function-drift.sh` compared only `index.ts`/`index.tsx`, so a function importing a sibling helper could drift in that file and still report `match`. Now diffs the whole function tree recursively (excluding `.DS_Store`) and names the differing files per side; verified by planting a sibling file, which the old check would have called clean. **(3) P2, over-broad exemption:** the `server` and `make-server-de2b7016` no-source exemptions describe the production project's inventory only, but applied on any `--project-ref`, so a different project's genuinely missing source could have passed. Exemptions are now applied only for the production ref, with a printed note when disabled; the block was also moved above the function-list fetch so that behaviour is observable rather than hidden behind an early exit. Drift check re-verified green against production afterwards. |
