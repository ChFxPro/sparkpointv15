node_modules/
build/
dist/
.DS_Store
public/downloads/press/

# Supabase local temp
supabase/.temp/

# Large local video source files
src/assets/commconn/movs/*.mp4

.prerender-preview/
public/assets/behind_story/manifest.json

# Agent handoff scratch (drafts, prompts, lock junk) — never ship
.resourcehub/
.storyhub/

# Generated print/design collateral (PPTX, PDF exports)
output/

# Playwright CLI debug logs/snapshots
.playwright-cli/
.playwright-mcp/

# Local secrets for one-off scripts (e.g. SUPABASE_SERVICE_ROLE_KEY) — never commit
.env
.env.*
!.env.example

# Monthly raw CSV exports dropped for scripts/internal-reports/ingest.mjs —
# transient input, not source; re-exported from the platform each month.
scripts/internal-reports/incoming/

# Local-only seed / reference data — never serve or commit (may contain internal staff notes).
# The Resource Directory's runtime source of truth is src/data/resources.ts, not these files.
# Ported 2026-08-12 from the feat/resource-directory worktree before it was removed;
# nothing has ever been committed here, so this is preventive.
/public/databases/
